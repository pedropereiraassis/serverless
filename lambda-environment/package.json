{
  "name": "lambda-environment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "invoke-local": "sls invoke local --stage prod -f commit-message-scheduler --verbose",
    "invoke": "sls invoke -f commit-message-scheduler --verbose"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1315.0",
    "axios": "^1.3.3",
    "cheerio": "^1.0.0-rc.12",
    "env-var": "^7.3.0",
    "uuid": "^9.0.0"
  }
}
